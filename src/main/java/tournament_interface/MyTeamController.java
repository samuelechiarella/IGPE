package tournament_interface;

import javafx.fxml.FXML;
import javafx.scene.layout.VBox;

public class MyTeamController  {

	
	@FXML
    private VBox teamList;
 
    @FXML
    void initialize() {
    	
    	//Image selectedFile = team.getLogo();
        //Label l = new Label(team.getName(), new ImageView(selectedFile));
       //l.setMinSize(200, 50);
        //teamList.getChildren().add(l);
    }
}
